import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ngx-intl-tel-input-angular15-ionic7',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
